---
globs: *.tsx,*.ts
alwaysApply: false
---

## Types

- any → конкретные типы/generics (если возможно)
- unknown → type guards для сужения (если возможно)
- enums → const objects или union types
- Избегай (если возможно) type assertions (as/!) — use type guards.
- infer interfaces from Zod schemas
- Use utility types (Partial, Pick, Omit, etc)
- import type for type-only.

## Functions

- Arrow functions for conciseness
- default params
- RORO pattern for ≥3 args (accept/return objects)

## Patterns

- Discriminated unions for state machines
- Template literal types
- Conditional types
- Type guards for runtime checks.

Modern TS 2025

- Optional chaining (?.)
- Nullish coalescing (??)
- Satisfies operator
- Const assertions.

Анти-паттерны:
❌ improper generic constraints; 


